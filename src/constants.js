export const ROUTES = {
  DEFAULT: '/',
  LOGIN: '/login',
  REGISTER: '/register',
  DASHBOARD: '/dashboard',
};

export const SESSION_PROPERTY = {
  TOKEN: 'USER_TOKEN',
  EMAIL: 'USER_EMAIL',
};

export const API_ROUTES = {
  LOGIN: '/login',
  REGISTER: '/register',
}
